#include<bits/stdc++.h>
using namespace std;
int main(){ 

// vector<int>v {2,3,4,5};

// v.resize(10); 

// for(int i=0; i<v.size(); i++){ 
//     cout<<v[i]<<" "; 
//     //cout<<endl; 
// }


/* there is another thing that we can do here ! by declaring the vector with memory space that we want*/

vector<int> v(10); 

for (int i =0 ; i<v.size(); i++){ 
cout<<v[i]<<" "; 
//now it is showing the all indexed is now with 0 because it is storing 0 in all the indexes 

}


}